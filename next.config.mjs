/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['upload.wikimedia.org','images.pexels.com','avatars.githubusercontent.com','encrypted-tbn0.gstatic.com'],
      },
};

export default nextConfig;
